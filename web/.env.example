# 复制为 .env.local（已 gitignore）后填写真实值。
# 两个变量都**不能**加 NEXT_PUBLIC_ 前缀，否则会被内联进客户端 bundle。

# 摄取服务（FastAPI）地址。浏览器不再直连它，只有 Next 服务端的
# /api/v1/[...path] 代理会访问，所以填回环地址即可。
INGEST_API_URL=http://127.0.0.1:8794

# 摄取服务的访问令牌，必须与 _kb/.env 里的 KB_API_TOKEN 完全一致。
# 生成：openssl rand -hex 32
# 缺失时代理不会注入该头，后端一律 401 —— 症状是全站写侧接口失败。
KB_API_TOKEN=
